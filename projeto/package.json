{
  "name": "projeto",
  "version": "1.0.0",
  "main": "server.js",  // Use "server.js" como ponto de entrada do seu servidor
  "scripts": {
    "start": "node server.js",  // Esse é o comando para iniciar o servidor
    "test": "echo \"Error: no test specified\" && exit 1"  // Não é necessário para o deploy, mas mantivemos
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "express": "^4.21.1",
    "body-parser": "^1.20.1",  // Adicionando body-parser
    "cors": "^2.8.5",  // Adicionando cors
    "better-sqlite3": "^8.6.1"  // Adicionando better-sqlite3
  }
}
